# Architecture — Canonical Sources

This document is the single registry of **canonical implementations**. Before adding a component, service, route, or enum, search here and extend the listed file — do not create parallel implementations.

Update this doc when ownership changes.

## Frontend

| Domain | Canonical | Do not add |
|--------|-----------|------------|
| CRM home dashboard | [`ActivityDashboardPage.tsx`](../frontend/src/pages/ActivityDashboardPage.tsx) at `/` → `/dashboard` via [`dashboardApi.ts`](../frontend/src/services/dashboardApi.ts) | Multifamily deal `DashboardTab`; resurrecting orphaned `HomePage` |
| Lead detail page | [`UnifiedLeadCommandCenter.tsx`](../frontend/src/components/UnifiedLeadCommandCenter.tsx) at `/leads/:id` — **must** render with `key={leadId}` or `key={numericId}` in [`App.tsx`](../frontend/src/App.tsx) so queue advance remounts; list UIs fail-closed via [`leadScopedRows.ts`](../frontend/src/utils/leadScopedRows.ts), which drops explicit foreign `lead_id` values; command-center owners normalize trusted current-lead server rows that lack `lead_id` before merging local state | New detail pages, `PropertyDetailPage`-style layouts; rendering CC without a remount key |
| Activity logging UI | [`LogActivityModal.tsx`](../frontend/src/components/LogActivityModal.tsx) + [`LogNoteForm`](../frontend/src/components/LogNoteForm.tsx) / [`LogCallForm`](../frontend/src/components/LogCallForm.tsx) / [`LogEmailForm`](../frontend/src/components/LogEmailForm.tsx) | Inline log forms, `window.prompt`, direct `callLogService` from queue row actions |
| Activity timeline (UI) | [`LeadTimeline.tsx`](../frontend/src/components/LeadTimeline.tsx) via `commandCenterService.getTimeline` + `scopeRowsToLeadWithTotal` for paginated totals | New timeline components on alternate APIs; rendering entries whose `lead_id` explicitly differs from the active lead |
| Work queues (UI) | [`*Queue.tsx`](../frontend/src/components/) components + `queueService` | `HubSpotLeadViews`, embedded queue copies in pages |
| Queue bulk actions / multi-select | [`queueBulkActions.tsx`](../frontend/src/components/queueBulkActions.tsx) + [`useQueueSelection.ts`](../frontend/src/hooks/useQueueSelection.ts) + [`QueueTable.tsx`](../frontend/src/components/QueueTable.tsx) | Per-queue parallel bulk handlers or selection state |
| Queue pagination | [`pagination.ts`](../frontend/src/utils/pagination.ts) | Local page math in queue components |
| Queue context banners | [`deriveQueueContext.ts`](../frontend/src/utils/deriveQueueContext.ts) | Inline banner logic in detail pages |
| Lead status UI | [`LeadStatusSelector.tsx`](../frontend/src/components/LeadStatusSelector.tsx) | Inline status Select + reason fields |
| Motivation score (product) | [`MotivationSignalService`](../backend/app/services/motivation_signal_service.py) → `lead.motivation_score` + MotivationSignalsPanel | Second motivation formula; treating HubSpot SIGNAL_ADJUSTMENTS as motivation_score |
| Owners / phones / emails (CC + queues) | [`Contact`](../backend/app/models/contact.py) + [`PropertyContact`](../backend/app/models/property_contact.py) via [`ContactService`](../backend/app/services/contact_service.py); CC phone **display** reads `contacts[].phones` (full phone DTO via [`PhoneConfidenceService.serialize_contact_phone`](../backend/app/services/phone_confidence_service.py)); top-level `phones[]` is merge/sort for outreach + no-contact fallback; UI via [`PhoneRow.tsx`](../frontend/src/components/PhoneRow.tsx) | Dual Info-tab flat owner/phone editor; queue rows reading only `owner_first_name`/`phone_1` when contacts exist; skinny `{id,value,label}` phone dumps; stripping confidence in sidebar |
| Prior-owner stale contacts (after sale) | [`contacts_likely_prior_owner`](../backend/app/services/scoring_rubric.py) (recent-sale window + skip-trace before sale) on CC as `contacts_likely_prior_owner` / `contacts_stale_since`; gray-out only in [`PropertySidebar.tsx`](../frontend/src/components/lead-detail/PropertySidebar.tsx); history via [`lead_owner_snapshots`](../backend/app/models/lead_owner_snapshot.py) + [`owner_snapshot_service`](../backend/app/services/owner_snapshot_service.py) (`former_owner` role on replace; unique `recent_sale` per lead+sale); prefer capture on replace / first stale open; exclude `former_owner` from scoring/outreach phone-email SQL; clears when `date_skip_traced` ≥ sale or sale older than recent-sale window | Parallel contact systems; deleting contacts on gray-out; blocking mail/call solely because of gray-out; re-implementing sale vs skip-trace date math in the FE; inventing `recent_sale` snapshots of already-replaced (buyer) contacts |
| Organizations / LLC filings | [`Organization`](../backend/app/models/organization.py) + [`OrganizationParty`](../backend/app/models/organization_party.py) via [`OrganizationService`](../backend/app/services/organization_service.py) / [`EntityResolutionService`](../backend/app/services/entity_resolution_service.py); `org_type` includes `nonprofit` / `property_management`; entity-shaped names include LLC/Inc **and** management vehicles ([`owner_name_utils`](../backend/app/services/plugins/owner_name_utils.py)) | Scraping Illinois SOS; parallel LLC tables; putting managers only on flat lead fields |
| LLC → person resolution (IL) | [`EntityResolutionService`](../backend/app/services/entity_resolution_service.py): institutional name / IRS EO BMF ([`irs_eo.py`](../backend/app/services/entity_lookup/irs_eo.py)) nonprofit research first, then free [`IllinoisSosBulkProvider`](../backend/app/services/entity_lookup/ilsos_bulk.py); cold-mail gate via [`entity_owner_policy`](../backend/app/services/entity_owner_policy.py) + [`LeadScoringEngine`](../backend/app/services/lead_scoring_engine.py) — **residential** unresolved entity → `enrich_data` / research; **commercial** unresolved entity may cold-mail; preemptive promote + `ensure_researched` + legacy “LLC search” task retirement via [`entity_research_lifecycle_service`](../backend/app/services/entity_research_lifecycle_service.py) (hourly with `mark_tasks_overdue` + command-center view); CC shows `entity_research` + Refresh | Interactive ILSOS scraping; Google/Serp as primary nonprofit classifier; parallel mail-skip engines; Cook County plugins owning SOS/manager lookup; calling skip-trace vendors from entity resolution; treating HubSpot “LLC search” chores as Call Now |
| Skip-trace handoff | [`SkipTraceEnqueue`](../backend/app/services/skip_trace_enqueue.py) → manual `skip_trace_owner` task / `needs_skip_trace`; kanban **Skip Trace** (`skip_trace`) is the active work queue — **Move to Skip Trace** is enabled from `awaiting_skip_trace` (e.g. after a recent-sale hold ends) and only shows an already-done **In Skip Trace** control when already in `skip_trace`; completing the last open `skip_trace_owner` refreshes `date_skip_traced` and promotes to `mailing_no_contact_made`; recent-sale reconciliation holds the lead in `skip_trace` with no nurture action, schedules owner re-verification for the two-year mail-eligible date, and the hourly reconciler moves it to `awaiting_skip_trace` when that date arrives **or** heals post-hold `contacts_likely_prior_owner` leads that never had a hold task; `promote_awaiting_skip_trace_due_leaks` (same hourly path + [`scripts/promote_awaiting_skip_trace_due_leaks.py`](../backend/scripts/promote_awaiting_skip_trace_due_leaks.py)) auto-moves `awaiting_skip_trace` leads that still have a dated open due task into active `skip_trace` with an undated handoff; **Adjust for Recent Sale** moves only the earliest open task's due date (preserving its type/title and mirrors) or creates the scheduled task when none exists | Calling a skip-trace API from EntityResolutionService; separate status/task mutations in UI; duplicate enqueue paths; treating pre-sale contacts as current; using nurture for operational holds; activating recent-sale skip trace before its hold expires; treating `awaiting_skip_trace` as Move-to-Skip-Trace already-done; inventing a second post-hold pipeline outside SkipTraceEnqueue |
| Same-building lead duplicates | [`lead_dedup_service.run_duplicate_sentinel`](../backend/app/services/lead_dedup_service.py) / Celery `leads.run_duplicate_sentinel` / [`scripts/consolidate_duplicate_leads.py`](../backend/scripts/consolidate_duplicate_leads.py) (`--dry-run` / `--apply`); merge via `merge_lead_into_winner` + `pick_merge_winner` | One-off SQL merges by hard-coded lead id; parallel merge engines |
| Quick-action eligibility | [`action_eligibility.py`](../backend/app/services/action_eligibility.py) (SoT) + FE mirror [`actionEligibility.ts`](../frontend/src/utils/actionEligibility.ts); gray-out in [`RecommendedActionPanel.tsx`](../frontend/src/components/RecommendedActionPanel.tsx); `ActionNotApplicableError` for forced ineligible calls | Parallel per-button enable rules; bare `"Invalid request"` without `message` / `reason_code` |
| Mail readiness | Complete owner mailing address + current target not matching returned-mail history ([`validate_owner_mailing_address`](../backend/app/services/open_letter_contact_mapper.py)) + no active 730-day recent-sale hold + `recommended_action == 'mail_ready'` + [`MailQueueItem`](../backend/app/models/mail_queue_item.py) / Ready to Mail; one urgency-neutral **Direct Mail** queue action; enqueue history via [`MailEnqueueAttempt`](../backend/app/models/mail_enqueue_attempt.py); staged → pending follow-up task (`due_date` null) via [`mail_task_lifecycle_service`](../backend/app/services/mail_task_lifecycle_service.py); recent-sale reconciliation moves due mail/outreach work to the eligible date in both task stores and HubSpot; send sets due +7d; all enqueue callers surface durable per-lead outcomes through `MailEnqueueResultDialog` / `formatEnqueueSummary`; soft no-op via `already_queued` | Property-address fallback for owner mail; blanket block on any non-empty `returned_addresses`; competing `up_next_to_mail` as mailer status; score dampening while queued; treating HTTP 201 as proof every requested lead was staged |
| Today's Action (due work) | [`QueueService.get_todays_action`](../backend/app/services/queue_service.py) — open **`LeadTask`** with `due_date <= today` (sorted by `lead_score`); excludes `awaiting_skip_trace` (skip-trace staging); recent-sale direct-mail work leaves only after canonical task reconciliation moves its due date; not bare `follow_up_now` | Membership via CRM `tasks` EXISTS; RA-only membership; hiding a whole recently-sold lead while unrelated work remains due; mixing proposal queues into due work; treating `awaiting_skip_trace` dated chores as salesperson Today's Action |
| API client pattern | Domain modules like [`condoFilterApi.ts`](../frontend/src/services/condoFilterApi.ts), [`leadApi.ts`](../frontend/src/services/leadApi.ts) | New exports appended to monolithic [`api.ts`](../frontend/src/services/api.ts) |
| Shared formatters | [`helpers.ts`](../frontend/src/utils/helpers.ts) (extend as needed) | Local `formatDate` / `humanize` copies in components |

## Backend

| Domain | Canonical | Do not add |
|--------|-----------|------------|
| CRM activity KPIs / goals | [`activity_dashboard_service.py`](../backend/app/services/activity_dashboard_service.py) + [`dashboard_controller.py`](../backend/app/controllers/dashboard_controller.py) at `/api/dashboard/*` → `user_activity_goals` + `LeadTimelineEntry` counts | Multifamily `DashboardService` / deal financial dashboard; parallel KPI aggregators |
| Unified scoring + recommended action | [`LeadScoringEngine`](../backend/app/services/lead_scoring_engine.py) + [`scoring_rubric.py`](../backend/app/services/scoring_rubric.py) via [`refresh_lead_scoring`](../backend/app/services/lead_refresh.py) | `DeterministicScoringEngine`, `ActionEngineService`, or second writers to `leads.lead_score` / `leads.recommended_action` |
| Pipeline status | `lead.lead_status` via lead_status_service / LeadStatusSelector | Treating `hubspot_deal_stage` as editable pipeline status (it is a read-only HubSpot mirror) |
| Live `leads.lead_score` | [`LeadScoringEngine.persist()`](../backend/app/services/lead_scoring_engine.py) — same value as latest `lead_scores.total_score` | Second writer to `leads.lead_score` |
| Per-phone confidence | [`PhoneConfidenceService`](../backend/app/services/phone_confidence_service.py) → `contact_phones.confidence_score` (HubSpot primary + annotations: WN/NIS/confirmed/family); outreach picks confidence then HubSpot-primary preference — not alphabetical; all contact phone API dumps use `serialize_contact_phone` | Parallel phone tracking tables; hand-built phone dicts that omit `confidence_score`; alphabetical phone tie-break |
| HubSpot contact refresh | [`HubSpotContactSyncService`](../backend/app/services/hubspot_contact_sync_service.py); mailing enrich via [`HubSpotMatcherService._apply_hubspot_mailing_addresses`](../backend/app/services/hubspot_matcher_service.py) — HubSpot primary `address`/city/state/zip fill empty mailing; `additional_addresses` promotes to mailing when empty/incomplete (same street), else `address_2` (Additional Address); backfill [`sync_hubspot_mailing_from_contacts.py`](../backend/scripts/sync_hubspot_mailing_from_contacts.py) | Ad-hoc contact API fetch outside enrich/backfill; writing HubSpot alts into `returned_addresses`; applying orphan city/state/zip without a primary street |

| Score history / audit API | [`LeadScoringEngine`](../backend/app/services/lead_scoring_engine.py) → `lead_scores` table (append-only) | Separate scoring engine writing different scores |
| Recommended action (live) | [`LeadScoringEngine`](../backend/app/services/lead_scoring_engine.py) → `leads.recommended_action` (same as `lead_scores.recommended_action`) | Parallel action engines or unmapped RA enums |
| Lead timeline (read/write) | [`command_center_controller.py`](../backend/app/controllers/command_center_controller.py) + [`CallLogService`](../backend/app/services/call_log_service.py) → `LeadTimelineEntry` | Duplicate `GET /api/leads/:id/timeline` handlers; injecting Interaction rows into CC |
| HubSpot activity → CC timeline | [`HubSpotTimelineImportService`](../backend/app/services/hubspot_timeline_import_service.py) → `LeadTimelineEntry` | Writing HubSpot activities only to `Interaction` for product UI |
| Lead quick briefing (CC) | [`LeadBriefingService`](../backend/app/services/lead_briefing_service.py) → `POST /api/leads/:id/briefing` (persists latest on `leads.quick_briefing`; Refresh revises); included on CC payload; UI [`LeadBriefingPanel.tsx`](../frontend/src/components/LeadBriefingPanel.tsx) | Parallel OpenAI client; auto-running briefings on every CC open; version history store |
| Lead open tasks (CC + queues) | [`LeadTask`](../backend/app/models/lead_task.py) via [`LeadTaskService`](../backend/app/services/lead_task_service.py) (incl. `hubspot_task_id`); next-action invariant via [`next_action_invariant.py`](../backend/app/services/next_action_invariant.py). HubSpot task pull is **not** a user-path side effect (`auto_sync_lead_if_stale` retired; CC GET / status PATCH local-only). Catch-up: Celery Beat, webhooks, or explicit `POST .../hubspot-sync` (`include_tasks=True`). Inbound sync must not complete an already-open LeadTask. | UNION of CRM `tasks` into CC `open_tasks` or queue membership; HubSpot auto-sync / stale-on-view pull; completing open LeadTasks from inbound HubSpot |
| Sale-date verification / Cook County enrichment | [`cook_county_enrichment_service.py`](../backend/app/services/cook_county_enrichment_service.py) + [`DataSourceConnector`](../backend/app/services/data_source_connector.py) → `data_sources` / `enrichment_records`; catalog self-heal on startup + `/api/health` FAIL if still missing; on-demand `POST .../sale-date-verification` (CC sidebar shows Verify + last-checked even when sale is **None**, including `no_results`); hourly `cook_county.backfill_sale_dates` (assessor-focused, Redis cursor) plus full-plugin `cook_county.backfill_enrichment`; missing PIN → sidebar **Look up PIN** via property-match preview/approve or `research_missing_pin` (not when `reason=incomplete_address`) | Parallel sale-verify services; mass-stamping all historical sales; silent empty `data_sources` catalog; hiding Verify when sale display is empty |
| Property address completeness | [`property_address_service.py`](../backend/app/services/property_address_service.py) — street + city + state + ZIP; parse then Cook street-only GIS; wired on HubSpot matcher (create + `enrich_lead_from_deal`) / quick-add / sheets / property match review-approve / ingestion dedup+GIS / lead merge; hourly Celery `property_address.heal_incomplete` (Redis cursor); one-shot [`heal_incomplete_property_addresses.py`](../backend/scripts/heal_incomplete_property_addresses.py) | Parallel completers; street-only placeholders without completer; creating `research_missing_pin` for incomplete situs; skipping Beat heal for backlog |
| Interaction timeline (CRM) | `GET /api/leads/:id/interaction-timeline` in [`interaction_controller.py`](../backend/app/controllers/interaction_controller.py) — **frozen for CC/product** | Overlapping URL with command-center timeline; new product consumers |
| Work queues (API) | [`queue_service.py`](../backend/app/services/queue_service.py) at `/api/queues/*` | `/api/properties/views/*` (301 → queues; legacy only) |
| Lead list/detail API | [`property_controller.py`](../backend/app/controllers/property_controller.py) at `/api/properties/*` | Logic in stub [`lead_controller.py`](../backend/app/controllers/lead_controller.py) |
| Controller error handling | Shared decorator in `app/controllers/decorators.py` (target) | Per-file `handle_errors` copies |
| Unified score + action refresh | [`refresh_lead_scoring()`](../backend/app/services/lead_refresh.py) | Ad-hoc score-only or action-only refresh at mutation sites |
| One-off scripts | Thin wrappers calling service layer (e.g. [`rescore_all.py`](../backend/scripts/rescore_all.py)) | Inlined SQL/field lists duplicating services |

## API routes (quick reference)

| Concern | Route | Handler |
|---------|-------|---------|
| CRM activity dashboard | `GET /api/dashboard/activity`, `PUT /api/dashboard/goals` | activity dashboard service |
| Lead detail (command center) | `GET /api/leads/:id/command-center` | command center controller |
| Lead activity timeline | `GET /api/leads/:id/timeline` | command center → `{ entries, total, page }` |
| Lead quick briefing | `POST /api/leads/:id/briefing` | LeadBriefingService → persist `leads.quick_briefing`; `{ bullets[5], mode, generated_at, updated_at, ... }` (revise when saved exists) |
| CRM interaction timeline | `GET /api/leads/:id/interaction-timeline` | interaction controller → `{ timeline }` |
| Log note / call | `POST /api/leads/:id/notes`, `/calls` | CallLogService |
| Sale-date verification | `POST /api/leads/:id/sale-date-verification` | cook_county_enrichment_service (queue or sync) |
| PIN sale history (CC Info) | `sale_history` on `GET /api/leads/:id/command-center` | `list_parcel_sale_history` in cook_county_assessor (cache, then Socrata) — not CRM timeline |
| Work queues | `GET /api/queues/*` | queue controller |
| Lead score history | `GET /api/lead-scores/:id` | lead score controller (LeadScoringEngine) |

## Migration rules

1. **Extend or delete** — replacing behavior means deleting the old file in the same PR (or a tracked follow-up within one release).
2. **One writer per column** — only one code path updates persisted derived fields (`lead_score`, `recommended_action`).
3. **Stubs are temporary** — re-export shims (`LeadDetailPage`, `lead_controller`) must not outlive one release; track removal in PR description.

## Known dual-path follow-ups

Active gaps where product UI can miss data that exists on a parallel path (fix in separate PRs; do not expand these paths):

1. **Flat owner/phone on All Properties / marketing** — queues enrich via contacts; list/marketing UIs still lean on flat `owner_*` / `phone_*`.
2. **`up_next_to_mail` vs `mail_ready` + `MailQueueItem`** — legacy flag still OR’d into awaiting-mail / lifecycle for uncleared rows.

~~Former queue task membership vs CC `open_tasks`~~ — closed: Today’s Action, Follow-Up Overdue (task branch), No Next Action, and scoring open-task counts are **LeadTask-only**. CRM `tasks` remain write-through mirrors for HubSpot identity, not membership.
~~Former Follow-Up Overdue CRM `tasks` EXISTS~~ — closed: overdue open `LeadTask` **or** `follow_up_now` + stale contact; no CRM EXISTS.
~~Former HubSpot activity → CC timeline~~ — closed: live convert / incremental webhook call `HubSpotTimelineImportService.sync_lead_from_interactions` after writing Interactions; historical backfill via `scripts/backfill_hubspot_interactions_to_timeline.py`. Interactions remain for scoring/signals / frozen CRM timeline.
## Related specs

Feature specs live in [`.kiro/specs/`](../.kiro/specs/). When a spec task says "remove file", deletion is part of **done**, not optional cleanup.
