[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    performance: timing/SLA regression tests (run via pytest -m performance)
    integration: tests requiring external services or full migration DB setup
